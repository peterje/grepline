---
"@grepline/cli": patch
---

create and reuse sanitized per-issue workspaces under the configured root, enforce root-safe workspace paths, and run lifecycle hooks with the spec's fatal and best-effort failure handling.
