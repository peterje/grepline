---
"@grepline/cli": patch
---

load workflow front matter and prompt templates into typed runtime config, validate startup-critical settings, and render issue prompts strictly with a minimal fallback prompt.
