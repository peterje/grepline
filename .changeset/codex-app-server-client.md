---
"@grepline/cli": patch
---

launch and manage codex app-server sessions inside validated workspaces, normalize streamed turn events and failures, and auto-resolve the v0 approval policy while hard-failing on user input requests.
